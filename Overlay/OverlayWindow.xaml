<Window x:Class="OwTranslateLite.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Width="520"
        Height="180">
    <Window.Resources>
        <SolidColorBrush x:Key="ReplyComboForegroundBrush" Color="#F5F7FA"/>
        <SolidColorBrush x:Key="ReplyComboAccentBrush" Color="#64D2FF"/>
        <SolidColorBrush x:Key="OverlayMutedBrush" Color="#A7AFBA"/>
        <SolidColorBrush x:Key="OverlayPanelBrush" Color="#D40B0C10"/>
        <SolidColorBrush x:Key="OverlayInputBrush" Color="#E0161820"/>
        <SolidColorBrush x:Key="OverlayLineBrush" Color="#33FFFFFF"/>

        <Style x:Key="ReplyComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="{StaticResource ReplyComboForegroundBrush}"/>
            <Setter Property="Background" Value="#171A22"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#253044"/>
                    <Setter Property="Foreground" Value="{StaticResource ReplyComboAccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ReplyComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Foreground" Value="{StaticResource ReplyComboForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="#4464D2FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ReplyComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="Chrome"
                                                CornerRadius="13"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <ContentPresenter Margin="8,0,18,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Center"
                                                                  RecognizesAccessKey="True"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"/>
                                                <Path Data="{StaticResource IconChevronDown}"
                                                      Stroke="{StaticResource ReplyComboAccentBrush}"
                                                      StrokeThickness="2"
                                                      Stretch="Uniform"
                                                      Width="11"
                                                      Height="11"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border Background="#F0141720"
                                        BorderBrush="#4464D2FF"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="4"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="150">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="FloatingPanel"
            Background="{StaticResource OverlayPanelBrush}"
            BorderBrush="#335C9DFF"
            BorderThickness="1"
            CornerRadius="14"
            Padding="14">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="0" Opacity="0.32"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition x:Name="DragHandleRow" Height="20"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="DragHandle" Cursor="SizeAll" Background="Transparent" Margin="-4,-4,-4,8">
                <Grid>
                    <Border Width="76"
                            Height="4"
                            CornerRadius="2"
                            Background="#4464D2FF"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>
                    <TextBlock Text="drag"
                               Foreground="{StaticResource OverlayMutedBrush}"
                               FontSize="9"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Opacity="0.65"/>
                </Grid>
            </Border>

            <ScrollViewer x:Name="TranslationScrollViewer"
                          Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="RecordList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Border Grid.Row="0"
                                        Background="#1E64D2FF"
                                        BorderBrush="#3364D2FF"
                                        BorderThickness="1"
                                        CornerRadius="9"
                                        Padding="7,2"
                                        HorizontalAlignment="Left"
                                        Margin="0,0,0,5">
                                    <TextBlock Text="{Binding Speaker}"
                                               Foreground="#D7F3FF"
                                               FontSize="11"
                                               FontWeight="SemiBold"/>
                                </Border>
                                <TextBlock Grid.Row="1"
                                           Text="{Binding TranslatedText}"
                                           Foreground="#F5F7FA"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"/>
                                <TextBlock Grid.Row="2"
                                           Text="{Binding SourceText}"
                                           Foreground="{StaticResource OverlayMutedBrush}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           Opacity="0.68"
                                           Margin="0,3,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <primitives:Thumb x:Name="ResizeGrip"
                              Grid.RowSpan="3"
                              Panel.ZIndex="20"
                              Width="24"
                              Height="24"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Bottom"
                              Cursor="SizeNWSE">
                <primitives:Thumb.Template>
                    <ControlTemplate TargetType="{x:Type primitives:Thumb}">
                        <Grid Background="Transparent">
                            <Border Width="15"
                                    Height="15"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    BorderBrush="#5564D2FF"
                                    BorderThickness="0,0,2,2"
                                    CornerRadius="0,0,4,0"/>
                        </Grid>
                    </ControlTemplate>
                </primitives:Thumb.Template>
            </primitives:Thumb>

            <Border x:Name="ReplyInputPanel"
                    Grid.Row="2"
                    Panel.ZIndex="10"
                    Visibility="Visible"
                    Margin="0,10,30,0"
                    Padding="5"
                    Background="#A0101116"
                    BorderBrush="{StaticResource OverlayLineBrush}"
                    BorderThickness="1"
                    CornerRadius="18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="82"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox x:Name="ReplyTargetCombo"
                              Grid.Column="0"
                              Style="{StaticResource ReplyComboBoxStyle}"
                              BorderThickness="1"
                              Background="{StaticResource OverlayInputBrush}"
                              VerticalContentAlignment="Center">
                        <ComboBoxItem Content="自动" Tag="auto"/>
                        <ComboBoxItem Content="英语" Tag="en"/>
                        <ComboBoxItem Content="日语" Tag="ja"/>
                        <ComboBoxItem Content="韩语" Tag="ko"/>
                    </ComboBox>
                    <TextBox x:Name="ReplyInputBox"
                             Grid.Column="2"
                             BorderThickness="0"
                             Background="{StaticResource OverlayInputBrush}"
                             Foreground="#F5F7FA"
                             CaretBrush="#64D2FF"
                             Padding="10,5"
                             VerticalContentAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
